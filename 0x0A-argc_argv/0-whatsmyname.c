#include "holberton.h"
#include <stdio.h>

/**
 * main - prints program name followed by a new line
 *
 * @argc : standard argc
 * @argv : standard argv
 *
 * Return: 0
 */

int main(int __attribute__((__unused__))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
